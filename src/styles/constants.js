import * as metrics from './metrics';

// HEADER
export const HEADER_HEIGHT = metrics.WINDOW_HEIGHT * 0.12;
export const HEADER_CONTENT_WIDTH = metrics.WINDOW_WIDTH * 0.75;
export const HEADER_LOGO_SIZE = 42;

// TOP DIVS
export const TOP_DIV_FONT_SIZE = 135;
export const MOBILE_TOP_DIV_FONT_SIZE = 55;
